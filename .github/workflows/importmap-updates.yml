name: Importmap updates
on:
  schedule:
    - cron: "0 9 * * 1"   # Mondays 09:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - uses: thoughtbot/importmap-update@45a21e82c7ca104d451d63c8db1c4aecdf2f3bd6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          rails-root: ${{ github.workspace }}
